<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
  	<meta name='viewport' content='width=device-width'>

    <title>{{ site.title }}</title>
    <meta name='description' content="{{ site.description }}">
    <meta name="theme-color" content="#ffffff"/>

    <!-- <meta property="og:url" content="{{ url }}" /> -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content="{{ title }}" />
    <meta property="og:description" content="{{ site.description }}" />
    <meta property="og:image" content="{{ site.url }}/assets/img/posts/{{ image }}" />

    <!-- https://rknight.me/please-expose-your-rss/ -->
    <link rel="alternate" type="application/rss+xml" title="{{ site.title }}" href="{{ site.rss }}" />
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/img/favicon/favicon-32x32.png">
    <link rel="apple-touch-icon" href="/assets/img/favicon/apple-touch-icon.png">
    <link rel="manifest" href="/manifest.json" />

    <!--
    <link href="https://unpkg.com/prismjs@1.20.0/themes/prism-okaidia.css" rel="stylesheet">
    <link href="/assets/styles/tailwind.css" rel="stylesheet" />
    <link href="/assets/styles/index.css" rel="stylesheet" />
    -->

    <!-- https://datatables.net/ -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <link rel="stylesheet" href="https://cdn.datatables.net/2.0.7/css/dataTables.dataTables.css" />
    <script src="https://cdn.datatables.net/2.0.7/js/dataTables.js"></script>
    <script>
      // const isDarkMode = () => localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches);
      // if (isDarkMode()) {
      //   document.documentElement.classList.add('dark')
      // } else {
      //   document.documentElement.classList.remove('dark')
      //}

      // https://datatables.net/
      $(document).ready( function () {
        $('#df1').DataTable({
          paging: false,  // https://datatables.net/reference/option/paging
          searching: false,
          info: false,
        });
        $('#df2').DataTable() // '#myTable', {
          // none of this works? If it's possible to just draw tables that are only as
          // wide as they need to be, I have no idea how.
          // autoWidth: false,  // https://datatables.net/reference/option/autoWidth
          // columnDefs: [{width: 5, targets: 0}],
          //fixedColumns: true,
        // });
      } );
    </script>

  </head>

  <body class="dark:text-white dark:bg-black">

    <h1>North & South Omaha Recovery Grant Program (NSORG)</h1>

    <p><a href="https://opportunity.nebraska.gov/programs/recovery/nsorg/">Official NSORG website</a>
    of the Nebraska Department of Economic Development.</p>

    <p>This repository is additional resources by Open/Nebraska volunteers.
    If you discover any errors in this website, or want to request new features, please
    <a href="https://github.com/opennebraska/nsorg/issues">create an Issue</a>.</p>

    <p>{{proposal_count}} proposals were submitted for project budgets totalling {{total_budget}}.
    For those proposals, {{total_requested}} was requested. {{total_awarded}} was awarded.</p>

    <h2>Awards by Category</h2>
    {{ df1 }}
    
    <h2>All Proposals</h2>
    {{ df2 }}

    <!--
    <script>
      document.getElementById("toggleDarkMode").addEventListener("click", function() {
        if (isDarkMode()) {
          localStorage.theme = 'light'
          document.documentElement.classList.remove('dark')
        } else {
          localStorage.theme = 'dark'
          document.documentElement.classList.add('dark')
        }
      });
    </script>
    -->
  </body>
</html>